<source>
  @type  forward
  @label @DEFAULT
  @id in_forward
  port  24224
  bind 0.0.0.0
</source>

<source>
  @type prometheus
  bind 0.0.0.0
  port 24231
  metrics_path /metrics
</source>

<source>
  @type prometheus_output_monitor
  interval 10
</source>

<label @FLUENT_LOG>
  <match fluent.*>
    @type stdout
    @id out_fluent_log
  </match>
</label>

<label @DEFAULT>
  <match **>
    @type stdout
    @id out_default
  </match>
</label>
